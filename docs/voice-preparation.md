# Voice preparation

Only clone a voice with documented consent. Record clean mono speech without music or reverb, retain
the original reference, language, style, speed, pitch, provider/model, and consent acknowledgment.
The UI rejects reference uploads without consent and offers deterministic tone preview in mock mode.
External TTS environments are administrator-installed and never receive credentials in logs.
