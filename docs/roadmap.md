# Roadmap

The exercised CPU mock slice is the reliability base. Next milestones are real ComfyUI workflow
fixtures, WanGP headless execution, Ollama planning, provider-level cancellation, black/freeze/silence
QA, audio ducking and subtitle muxing, worker heartbeats/VRAM admission, and Playwright in CI. After
that, run independent 12 GB profiling on GPU 0 and GPU 1 for selected video/image/TTS/lip-sync/
interpolation providers. Four-to-eight-minute production requires chunking, storage management, and
long-run recovery evidence; it is not promised by the current build.
